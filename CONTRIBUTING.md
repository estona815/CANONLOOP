# Contributing

Keep changes small, run all validation commands from the README, never commit credentials, and do
not present Replay or Mock evidence as a live provider or Backblaze B2 run. New media must include
an origin, rights status, SHA-256, and submission-use declaration in `ASSET_MANIFEST.json`.

