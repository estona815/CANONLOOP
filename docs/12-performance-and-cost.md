# Performance and Cost

Replay is static-first and incurs no model cost. Live defaults are two concurrent jobs, two repairs,
a 180-second timeout, and a USD 5 estimated-cost cap. Unknown provider pricing remains unknown; the
application does not invent costs. Images use Next.js optimization and stable aspect-ratio frames.

