# Product Requirements

The judge can run a no-account guided Replay, inspect canon anchors and format briefs, compare a
rejected and repaired vertical story, inspect the run lineage and storage index, and download a safe
campaign ZIP. Live mode must fail closed without credentials and explicit cost confirmation.

